# Example twist_mux input priorities; adapt to your installed twist_mux package format.
inputs:
  nav:
    topic: /cmd_vel_nav
    timeout: 0.5
    priority: 50
  align:
    topic: /cmd_vel_align
    timeout: 0.3
    priority: 80
  estop:
    topic: /cmd_vel_estop
    timeout: 0.1
    priority: 255
output_topic: /cmd_vel
